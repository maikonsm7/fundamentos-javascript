// Calcular a área de uma circunferência -> Pi * raio * raio

let raio = 3
let resultado = Math.PI * Math.pow(raio, 2) // Math.pow -> exponenciação

console.log("O valor da área é: " + resultado.toFixed(2) + "m2")